version: '3.8'
services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_SECRET_KEY=your_super_secret_key
      - ADMIN_USER=admin
      - ADMIN_PASS=password
    volumes:
      - print-calculator-data:/data

volumes:
  print-calculator-data:
